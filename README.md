# jdbc-yugabyte-universe-failover
A Java JDBC DataSource that routes between two Yugabyte universe connection pools and fails over when the active universe is down — SmartDriver still handles nodes inside each universe.
